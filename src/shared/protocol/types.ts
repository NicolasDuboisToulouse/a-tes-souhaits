export type ErrorMessage = {
  errorMessage: string;
};

export type LoginInfo = {
  userName: string;
  password: string;
};

export type User = {
  userName: string;
};

export type HelloReq = {
  message: string;
};

export type HelloResp = {
  message: string;
};
